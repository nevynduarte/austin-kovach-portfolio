import * as T from 'three';
import {RoundedBoxGeometry} from 'three/addons/geometries/RoundedBoxGeometry.js';
const mat=(color,metalness=0,roughness=.45)=>new T.MeshStandardMaterial({color,metalness,roughness});
const steel=mat('#c9cfce',.92,.23), dark=mat('#232b2c',.15,.38), rubber=mat('#151a1b',0,.83), green=mat('#4b6251',.25,.4), white=mat('#eaede9',.2,.35), red=mat('#b32b2e',.12,.36), yellow=mat('#e9b72a',.25,.4);
const glass=new T.MeshPhysicalMaterial({color:'#b0cecf',metalness:0,roughness:.14,transparent:true,opacity:.24,side:T.DoubleSide,depthWrite:false});
function mesh(g,m,pos=[0,0,0],rot=[0,0,0]){const o=new T.Mesh(g,m);o.position.set(...pos);o.rotation.set(...rot);return o;}
function box(w,h,d,m,pos,bevel=.04){return mesh(new RoundedBoxGeometry(w,h,d,3,Math.min(bevel,w/3,h/3,d/3)),m,pos);}
function cyl(r,h,m,pos,axis='y',r2=r){return mesh(new T.CylinderGeometry(r,r2,h,64),m,pos,axis==='x'?[0,0,Math.PI/2]:axis==='z'?[Math.PI/2,0,0]:[0,0,0]);}
function ring(r,t,m,pos,axis='y'){return mesh(new T.TorusGeometry(r,t,10,80),m,pos,axis==='x'?[0,Math.PI/2,0]:axis==='y'?[Math.PI/2,0,0]:[0,0,0]);}
function lathe(points,m,pos=[0,0,0]){return mesh(new T.LatheGeometry(points.map(p=>new T.Vector2(...p)),80),m,pos);}
function tube(r,h,t,m,pos){return lathe([[r-t,0],[r,0],[r,h],[r-t,h],[r-t,0]],m,pos);}
function part(root,name,vector,description){let p=new T.Group();p.name=name;p.userData={part:true,explode:vector,description,provenance:'Image-guided reconstruction; inferred dimensions and hidden surfaces'};root.add(p);return p;}
function model(){let g=new T.Group();g.userData={units:'relative',source:'Austin Kovach portfolio PDF',accuracy:'visual reconstruction, not CAD'};return g;}
function coffee(){let r=model(),p;
 p=part(r,'Rubber base',[0,-.5,0],'A durable base protects the mug at its contact surface.');p.add(lathe([[0,0],[.46,0],[.56,.06],[.58,.12],[.58,.18],[.49,.18],[0,.18]],rubber));
 p=part(r,'Outer mug',[0,0,0],'The outer mug combines a metal rim with grippier surfaces. Its hollow form is visible in the original exploded drawing.');p.add(tube(.575,1.2,.045,steel,[0,.12,0]));p.add(tube(.59,.98,.025,green,[0,.15,0]));
 p=part(r,'Grip twist',[0,.65,0],'A textured twist ring makes the rotational interaction easier to grip.');p.add(tube(.60,.24,.045,green,[0,1.32,0]));for(let i=0;i<16;i++){let a=i*Math.PI/8;let o=box(.145,.15,.035,steel,[Math.sin(a)*.602,1.44,Math.cos(a)*.602],.028);o.rotation.y=a;p.add(o);let inset=box(.12,.125,.04,green,[Math.sin(a)*.615,1.44,Math.cos(a)*.615],.024);inset.rotation.y=a;p.add(inset);}
 p=part(r,'Internal mug',[0,1.3,0],'The ribbed internal mug nests inside the outer body. The nested geometry is reconstructed from the exploded view.');p.add(tube(.48,1.72,.035,green,[0,.22,0]));p.add(cyl(.475,.055,green,[0,.25,0]));p.add(tube(.50,.12,.04,steel,[0,1.82,0]));for(let i=0;i<16;i++)p.add(ring(.482,.012,green,[0,.33+i*.09,0]));
 p=part(r,'Filter basket',[0,2.05,0],'A removable basket sits within the top of the internal mug.');p.add(tube(.434,.35,.028,green,[0,1.53,0]));p.add(cyl(.411,.022,dark,[0,1.54,0]));for(let i=0;i<8;i++)for(let j=0;j<8;j++){let x=(i-3.5)*.075,z=(j-3.5)*.075;if(x*x+z*z<.12)p.add(cyl(.009,.025,steel,[x,1.555,z]));}
 p=part(r,'Filter lid',[0,2.5,0],'The filter lid closes the basket below the main lid.');p.add(cyl(.46,.045,green,[0,1.98,0]));
 p=part(r,'Leak-proof lid',[0,3,0],'A broad lid provides a protected closure and a deliberate touch surface.');p.add(lathe([[0,2.06],[.49,2.06],[.57,2.1],[.59,2.16],[.59,2.27],[.54,2.32],[.46,2.32],[.46,2.27],[0,2.27]],green));p.add(ring(.515,.027,rubber,[0,2.08,0]));
 p=part(r,'Strap mount',[0,3.45,0],'An integrated mounting point supports a strap or carabiner.');p.add(ring(.085,.026,steel,[.26,2.34,0],'z'));return r;}
function mobility(){let r=model(),p;
 p=part(r,'Chassis and deck',[0,-.45,0],'Aluminum chassis and deck package the body, standing area, and three-wheel layout.');p.add(box(2.55,.13,.69,steel,[.3,.35,0]));p.add(box(1.35,.035,.58,rubber,[.87,.44,0]));for(let i=0;i<14;i++)p.add(box(.025,.015,.54,dark,[.25+i*.085,.465,0],.005));
 function wheel(x,z,group){group.add(cyl(.25,.15,rubber,[x,.25,z],'z'));group.add(cyl(.16,.157,steel,[x,.25,z],'z'));group.add(cyl(.12,.166,dark,[x,.25,z],'z'));for(let i=0;i<6;i++){let a=i*Math.PI/3;group.add(cyl(.014,.17,steel,[x+Math.sin(a)*.105,.25+Math.cos(a)*.105,z],'z'));}}
 p=part(r,'Front wheel pair',[-.35,-.2,0],'Two front wheels establish the stable three-wheel stance shown in the source renders.');wheel(-.72,.49,p);wheel(-.72,-.49,p);p.add(cyl(.047,1.0,steel,[-.72,.25,0],'z'));
 p=part(r,'Rear wheel',[.7,-.15,0],'A single rear wheel sits beneath the extended standing deck.');wheel(1.36,0,p);p.add(box(.5,.09,.23,dark,[1.35,.51,0]));
 p=part(r,'Body shell',[0,.7,-.42],'A faceted enclosure packages the storage volume. The design proposes recycled polypropylene panels.');let g=new RoundedBoxGeometry(.94,1.8,.8,2,.07),a=g.attributes.position;for(let i=0;i<a.count;i++){let y=a.getY(i),t=(y+.9)/1.8;a.setZ(i,a.getZ(i)*(1-.16*t));a.setX(i,a.getX(i)*(1-.2*t));}g.computeVertexNormals();p.add(mesh(g,white,[-.5,1.48,0]));p.add(box(.8,.15,.74,white,[-.5,.61,0]));
 p=part(r,'Storage door',[.8,.15,.45],'Lockable storage is accessible from the rider-facing side of the body.');p.add(box(.04,1.5,.64,dark,[.0,1.48,0]));p.add(box(.075,.045,.32,rubber,[.035,2.08,0]));
 p=part(r,'Lighting',[-.7,0,0],'Low-mounted front lighting follows the final render. Light construction is illustrative.');for(let z of [-.25,.25]){p.add(box(.09,.115,.21,dark,[-.98,.77,z]));const led=mat('#ffffff',0,.2);led.emissive.set('#ffffff');led.emissiveIntensity=2;p.add(box(.095,.038,.13,led,[-1.0,.78,z]));p.add(box(.096,.03,.055,yellow,[-1,.78,z+Math.sign(z)*.08]));}
 p=part(r,'Steering stem',[0,1.25,0],'The compact stem connects the handle-mounted interface to the vehicle.');p.add(cyl(.05,.25,dark,[-.48,2.46,0]));
 p=part(r,'Handlebar and controls',[0,1.75,0],'Handle-mounted acceleration, braking, phone holder, display, and USB charging organize the rider interface.');p.add(cyl(.045,1.12,dark,[-.48,2.61,0],'z'));for(let z of [-.48,.48])p.add(cyl(.071,.31,rubber,[-.48,2.61,z],'z'));p.add(box(.18,.065,.24,dark,[-.45,2.64,0]));p.add(box(.13,.008,.16,mat('#95b4ab',.25,.2),[-.45,2.68,0]));return r;}
function haven(){let r=model(),p;
 p=part(r,'Central support',[0,0,0],'A sculpted central column carries the canopy and rider interface.');p.add(lathe([[0,0],[.43,0],[.44,.06],[.28,.18],[.13,.5],[.13,1.58],[.23,1.83],[.49,2.04],[.52,2.11],[0,2.11]],white));
 p=part(r,'Rider display',[0,0,.85],'Rider information and tools are embedded in the central display.');p.add(box(.16,.58,.048,dark,[0,1.43,.165]));p.add(box(.12,.44,.014,mat('#8cbbc3',.1,.25),[0,1.45,.198]));
 p=part(r,'Perch table',[0,-.5,.5],'An integrated circular surface surrounds the central column.');p.add(cyl(.48,.065,white,[0,.78,0]));p.add(ring(.475,.012,yellow,[0,.793,0]));
 function canopy(name,y,w,d,vec){p=part(r,name,vec,'An extended rounded canopy provides coverage beyond the standard shelter footprint.');p.add(box(w,.08,d,white,[0,y,0],.04));p.add(box(w+.014,.023,d+.014,yellow,[0,y-.022,0],.01));}
 canopy('Lower canopy',2.12,4.9,1.46,[0,.65,0]);canopy('Upper canopy',2.28,4.7,1.3,[0,1.25,0]);
 p=part(r,'Solar panels',[0,1.7,0],'Two solar arrays are shown in the final canopy rendering.');for(let x of [-1.1,1.1]){p.add(box(1.22,.02,.85,mat('#152e4a',.45,.3),[x,2.335,0],.004));for(let i=0;i<14;i++)p.add(box(.008,.006,.82,steel,[x-.55+i*.085,2.35,0],.001));for(let j=0;j<5;j++)p.add(box(1.18,.006,.005,steel,[x,2.35,-.36+j*.18],.001));}
 for(const side of [-1,1]){const label=side<0?'Left':'Right',x=side*1.45;
 p=part(r,label+' enclosure',[side*.75,0,-.4],'Replaceable glazing and panel construction enable configurable shelter coverage.');p.add(box(1.53,.10,1.15,steel,[x,.05,0],.04));p.add(box(1.45,1.85,.055,dark,[x,1.04,-.52]));p.add(box(.025,1.9,1.06,glass,[x+side*.73,1.04,0],.006));for(let xx of [x-.73,x+.73])p.add(box(.035,1.97,.035,steel,[xx,1.035,-.52],.006));
 p=part(r,label+' sliding door',[side*1.1,0,.8],'Sliding-door access is part of the accessibility intent. This visualization is not a compliance certification.');p.add(box(1.40,1.88,.024,glass,[x,1.05,.52],.005));for(let xx of [x-.69,x+.69])p.add(box(.026,1.9,.035,steel,[xx,1.05,.52],.004));p.add(box(.024,.45,.06,dark,[x-side*.56,1.0,.55],.006));
 p=part(r,label+' bench',[side*.45,-.2,.55],'Bench modules are paired with each shelter enclosure.');p.add(box(1.23,.055,.39,mat('#eab129',.1,.43),[x,.55,-.1],.025));p.add(box(.55,.43,.35,steel,[x,.31,-.1]));for(let xx of [x-.3,x+.3])p.add(box(.028,.08,.33,steel,[xx,.61,-.1],.012));}
 return r;}
function wrench(){let r=model(),p,Y=1.3;
 p=part(r,'Housing and grip',[0,0,0],'A compact housing and refined grip preserve the original component architecture.');p.add(box(.95,.74,.72,dark,[-.27,Y,0],.16));let grip=box(.32,.97,.32,rubber,[-.35,.52,0],.11);grip.rotation.z=-.15;p.add(grip);p.add(box(.40,.12,.38,dark,[-.42,.05,0],.05));for(let z of [-.167,.167])p.add(box(.02,.70,.013,red,[-.30,.52,z],.006));p.add(box(.55,.25,.018,red,[-.25,Y+.03,.364],.025));p.add(box(.49,.20,.02,dark,[-.25,Y+.03,.379],.022));
 p=part(r,'Rear mode dial',[-.8,0,0],'A larger sculpted mode dial makes the rear control easier to operate.');p.add(cyl(.27,.09,red,[-.81,Y,0],'x'));for(let i=0;i<8;i++){let a=i*Math.PI/4;p.add(cyl(.067,.10,red,[-.83,Y+Math.sin(a)*.22,Math.cos(a)*.22],'x'));}p.add(cyl(.16,.115,red,[-.83,Y,0],'x'));
 p=part(r,'Hammer case',[.8,0,0],'The forward case encloses the retained hammer mechanism.');let h=lathe([[0,0],[.2,0],[.22,.08],[.29,.14],[.32,.3],[.35,.42],[.35,.64],[.32,.67],[.29,.64],[.29,.32],[.18,.12],[0,.12]],steel);h.rotation.z=-Math.PI/2;h.position.set(.14,Y,0);p.add(h);p.add(ring(.35,.028,dark,[.65,Y,0],'x'));
 p=part(r,'Detachable LED ring',[1.35,0,0],'A detachable light ring illuminates the working area.');p.add(ring(.245,.033,dark,[.8,Y,0],'x'));const led=mat('#ffffff');led.emissive.set('#ffffff');led.emissiveIntensity=2;for(let i=0;i<8;i++){let a=i*Math.PI/4;p.add(cyl(.018,.03,led,[.827,Y+Math.sin(a)*.245,Math.cos(a)*.245],'x'));}
 p=part(r,'Drive anvil',[1.9,0,0],'The half-inch drive is retained from the tool architecture; proportions here are illustrative.');p.add(cyl(.09,.20,steel,[.9,Y,0],'x'));p.add(box(.17,.12,.12,steel,[1.07,Y,0],.01));
 p=part(r,'Motor assembly',[.35,0,.8],'The existing internal motor package is a design constraint. Hidden geometry is approximated from the exploded drawing.');p.add(cyl(.235,.7,steel,[-.15,Y,0],'x'));for(let i=0;i<6;i++){let a=i*Math.PI/3;p.add(box(.58,.027,.055,steel,[-.13,Y+Math.sin(a)*.23,Math.cos(a)*.23],.01));}
 p=part(r,'Trigger',[.25,-.25,.5],'The trigger is positioned for a comfortable finger interaction.');p.add(box(.085,.24,.15,red,[-.07,.96,0],.028));
 p=part(r,'Air inlet',[0,-.85,0],'The inlet and valve package follow the handle axis in the original exploded drawing.');p.add(cyl(.075,.24,steel,[-.42,-.10,0]));for(let i=0;i<5;i++)p.add(ring(.074,.007,steel,[-.42,-.17+i*.03,0]));
 p=part(r,'Housing fasteners',[-1.3,0,0],'Four housing fasteners follow the visible rear assembly pattern.');for(let y of [-.27,.27])for(let z of [-.27,.27]){p.add(cyl(.023,.8,steel,[-.35,Y+y,z],'x'));p.add(cyl(.05,.04,steel,[-.79,Y+y,z],'x'));}return r;}
export const recipes={coffee,mobility,haven,wrench};
